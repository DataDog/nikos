---
- name: stat "/tmp/result/usr/include/linux/version.h"
  stat:
    path: "/tmp/result/usr/include/linux/version.h"
  register: stat_result
    
- name: check kernel headers were successfully unpacked
  assert:
    that: stat_result.stat.exists